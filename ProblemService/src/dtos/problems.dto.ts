// import { ITestCase } from "../models/problem.model";

// export interface CreateProblemDto {
//     title: string;
//     description: string;
//     difficulty: 'easy' | 'medium' | 'hard';
//     editorial?: string;
//     testcases: ITestCase[];
// }

// export interface UpdateProblemDto {
//     title?: string;
//     description?: string;
//     difficulty?: 'easy' | 'medium' | 'hard';
//     editorial?: string;
//     testcases?: ITestCase[];
// }

// we do not need these dtos if we can convert the zod types to the required types

// in the problem.validator we can do the same